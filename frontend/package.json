{
  "name": "ekt-assistant-ui",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "node server.mjs",
    "check": "node --check app.js && node --check ui-model.js && node --check server.mjs",
    "test": "node --test tests/*.test.mjs"
  }
}
