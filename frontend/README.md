# EKT AI Assistant — frontend

Автор UI: Moveath. Адаптивный интерфейс на HTML/CSS/JavaScript без зависимостей. Требуется Node.js 20+.

```powershell
cd frontend
npm start
```

Открыть http://localhost:5173. В отдельном терминале запустить существующий FastAPI backend на порту 8000 согласно его настройкам. UI работает и без backend, но запросы показывают сообщение о недоступности; вымышленные товары не отображаются.

`server.mjs` раздаёт только явно разрешённые файлы frontend и проксирует `POST /api/chat` и `GET /health` в существующий backend. По умолчанию `BACKEND_URL=http://127.0.0.1:8000`; при необходимости переменную можно задать в терминале. Секреты frontend не нужны. Локальный сервер слушает только loopback; для публикации потребуется отдельная production-конфигурация.

Поддержаны история последних 12 сообщений, четыре быстрых запроса, форматирование длинных ответов, карточки товаров, окно подробностей, характеристики без JSON, загрузка/ожидание ответа, пустая выдача, ошибки и повтор запроса. Черновик сохраняется при ожидании ответа. Enter отправляет, Shift+Enter переносит строку. Новый диалог очищает сообщения и список выбора. История и выбор живут только в памяти вкладки.

Карточки различают доступный, отсутствующий и неизвестный остаток, отмечают возможные аналоги и необходимость проверки совместимости. Цена, валюта и единица измерения не придумываются. `id` показывается как код товара; артикул и сертификат отображаются, если они действительно есть в `characteristics`. Для сертификатов разрешены только абсолютные HTTP(S)-ссылки без встроенных учётных данных. Окно «Подробнее» показывает предоставленные каталогом характеристики, а не вымышленную страницу товара.

Кнопка «Выбрать для корзины» открывает окно с количеством и предупреждением. Открытие и отмена ничего не отправляют. Только «Подтвердить выбор» передаёт `confirm_cart` в существующий API для повторной проверки наличия. При неизвестном остатке/единице предложено уточнение; при отсутствии — подбор аналога. «Ваш выбор» показывает подтверждённые позиции текущего диалога. Повторное подтверждение одной позиции заменяет её количество в этом списке.

API корзины ekt.kz в текущем backend не подключён. Интерфейс сообщает только о подтверждении выбора и не обещает добавление в реальную корзину. Новое сообщение закрывает старое предложение подтверждения.

Состояние реального успеха предусмотрено только для ответа `cart.status="confirmed"` и `cart.added_to_cart=true` после явного подтверждения. Переход в корзину появляется только при валидном `cart.cart_url` из этого ответа. Текущая схема `main.py` фиксирует `added_to_cart=false` и не возвращает `cart_url`, поэтому реальное добавление недоступно до реализации командой API корзины. Backend в рамках UI-изменений не менялся.

Запрос «Условия доставки» отправляется тому же ассистенту. В текущем API нет отдельного источника условий доставки: UI не подставляет вымышленные сроки или тарифы. Индикатор «Сервер на связи» означает ответ `/health`, а не гарантию доступности каталога и AI. API не потоковый; индикатор подготовки ответа означает ожидание, а не получение токенов.

Проверки без установки зависимостей:

```powershell
npm run check
npm test
```

Проверяемый сценарий: найти автомат → открыть «Подробнее» → «Выбрать для корзины» → проверить количество → отменить (запроса нет) → выбрать снова и подтвердить → увидеть «Выбор подтверждён» и честное уведомление, что товары не добавлены → открыть «Ваш выбор». Проверить пустую выдачу, сетевую ошибку/повтор, отсутствующий товар, аналог, Enter/Shift+Enter, длинный текст и новый диалог.

Адаптивная сетка перестраивает карточки в одну колонку на телефоне. Модальные окна используют native `dialog` с управлением фокусом и Escape; интерактивные элементы имеют видимый фокус, а анимации отключаются при `prefers-reduced-motion`.
